# 슬라이스 백로그 — 하나만 골라 2일 진행

각 slice는 **1~2개 파일에서 test-first 로 끝낼 수 있는 얇은 단위**입니다.
본인 상황(초·중급)에 맞게 하나를 고르세요. 여유가 있으면 하나 더.

| # | slice | 난이도 | 무엇을 |
| --- | --- | --- | --- |
| 1 | 사용 가드(음수·초과) | ★ (권장 시작) | `usePoints` 에 음수/잔액초과 가드 추가. `test/loyalty.test.js` 의 skip 2개 해제 → 구현. |
| 2 | 등급 확장(vip) | ★ | `EARN_RATE` 에 `vip` 10% 추가 + AC/테스트 작성. |
| 3 | 포인트 만료(expire) | ★★ | 획득 시각 기준 만료를 반영한 유효 잔액 계산을 **신규 구현**(greenfield). |
| 4 | 일일 적립 한도 | ★★ | 하루 적립 상한 초과분 컷 + 경계 테스트. |
| 5 | 사용 취소(환불) | ★★ | 사용 취소로 잔액 복구 + 이중 취소 방지. |

## 권장 경로
- **초급/시간 부족:** slice #1 — labs 의 test-first 패턴과 동일해 가장 안전합니다.
- **여유:** #1 완료 후 #3(만료)로 확장하면 "신규 기능 설계 → 스펙 → 테스트 → 구현" 전체를 경험합니다.

## 2일 매핑 (선택한 slice로 진행)
- Day1: 스펙 채택(S1) → User Story·AC(S2) → Context Manifest·리팩토링(S3) → Plan·Test Matrix(S4) → Spec Pack v1(S5)
- Day2: 하네스·로컬 게이트(S1~S3) → **test-first 구현**(S4~S5) → Custom Skill(S6) → 공유(S7)
